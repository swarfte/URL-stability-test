/// Named route identifiers used by the app's navigator. Spec §5 flow.
class AppRoutes {
  const AppRoutes._();

  static const String setup = '/';
  static const String progress = '/progress';
  static const String result = '/result';
}
